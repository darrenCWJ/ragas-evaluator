# Keep shared pins identical to the root requirements.txt so the app and the
# worker resolve the same versions. Update both files together.
fastapi>=0.115.0
uvicorn>=0.34.0
python-dotenv>=1.2.0
openai>=2.29.0
ragas>=0.4.3
psycopg2-binary>=2.9.0
langchain-text-splitters>=0.3.0
chromadb>=0.6.0
anthropic>=0.42.0
rapidfuzz>=3.0.0
