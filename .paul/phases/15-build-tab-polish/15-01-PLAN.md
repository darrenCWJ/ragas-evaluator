---
phase: 15-build-tab-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/build/DocumentList.tsx
  - frontend/src/components/build/ChunkConfigPanel.tsx
  - frontend/src/components/build/EmbeddingConfigPanel.tsx
  - frontend/src/components/build/RagConfigPanel.tsx
autonomous: true
---

<objective>
## Goal
Fix delete button error handling across all Build tab config panels, add help text/tooltips to all form fields, and verify PDF upload works at runtime.

## Purpose
Users can't tell if deletion succeeded or failed (silent catch blocks), and have no guidance on what each config field means. These UX issues block effective use of the Build pipeline.

## Output
- Delete buttons show error feedback on failure and reset confirmation state
- All config form fields have descriptive help text
- PDF upload verified working
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md

## Source Files
@frontend/src/components/build/DocumentList.tsx
@frontend/src/components/build/ChunkConfigPanel.tsx
@frontend/src/components/build/EmbeddingConfigPanel.tsx
@frontend/src/components/build/RagConfigPanel.tsx
</context>

<acceptance_criteria>

## AC-1: Delete buttons show error feedback
```gherkin
Given a user clicks "Yes" to confirm deletion of any config item
When the API call fails
Then an error message is displayed inline
And the confirmation UI resets so the user can retry or cancel
```

## AC-2: All config form fields have help text
```gherkin
Given a user views any config creation form (Chunk, Embedding, RAG)
When they see a form field
Then there is descriptive help text explaining the field's purpose, valid values, or typical usage
```

## AC-3: PDF upload works end-to-end
```gherkin
Given a user uploads a PDF file in the Build tab
When the file is processed
Then the text content is extracted and stored as a document
And the document appears in the document list
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Fix delete error handling in all Build tab panels</name>
  <files>frontend/src/components/build/DocumentList.tsx, frontend/src/components/build/ChunkConfigPanel.tsx, frontend/src/components/build/EmbeddingConfigPanel.tsx, frontend/src/components/build/RagConfigPanel.tsx</files>
  <action>
    In each of the 4 components, fix the delete handler's catch block:
    1. Add a `deleteError` state variable (string | null)
    2. In the catch block: set deleteError to the error message, AND reset confirmId/confirmDeleteId to null
    3. Display the deleteError inline near the delete button area (use existing error styling: bg-score-low/10 text-score-low)
    4. Clear deleteError when a new delete is attempted or after a timeout

    Pattern to apply in each component's handleDelete:
    ```
    catch (err) {
      setDeleteError(err instanceof Error ? err.message : "Delete failed");
      setConfirmDeleteId(null); // or setConfirmId(null) for DocumentList
    }
    ```
  </action>
  <verify>npm run build passes with no TypeScript errors</verify>
  <done>AC-1 satisfied: Delete failures show error message and reset confirmation state</done>
</task>

<task type="auto">
  <name>Task 2: Add help text to all config form fields</name>
  <files>frontend/src/components/build/ChunkConfigPanel.tsx, frontend/src/components/build/EmbeddingConfigPanel.tsx, frontend/src/components/build/RagConfigPanel.tsx</files>
  <action>
    Add a small help text line (text-xs text-text-muted) below each form label across all three config panels:

    **ChunkConfigPanel:**
    - Name: "A descriptive name for this chunking configuration"
    - Method: "recursive: splits by separators recursively | parent_child: creates parent-child chunk pairs | semantic: splits by semantic similarity | fixed_overlap: fixed-size chunks with overlap"
    - Chunk Size: "Target size in characters for each chunk (typical: 500-2000)"
    - Overlap: "Number of overlapping characters between chunks (typical: 50-200)"
    - Threshold: "Similarity threshold for semantic splitting (0.0-1.0, typical: 0.5)"
    - 2nd Pass Method: "Optional second pass to further refine chunks from the first pass"

    **EmbeddingConfigPanel:**
    - Name: "A descriptive name for this embedding configuration"
    - Type: "dense_openai: OpenAI API embeddings (best quality) | dense_sentence_transformers: local model embeddings (free, slower) | bm25_sparse: keyword-based sparse embeddings (no ML)"
    - Model Name: "Model identifier (e.g., text-embedding-3-small for OpenAI, all-MiniLM-L6-v2 for sentence-transformers)"
    - Params: "Optional JSON parameters for the embedding model"

    **RagConfigPanel:**
    - Name: "A descriptive name for this RAG configuration"
    - Embedding Config: "Which embedding model to use for retrieval"
    - Chunk Config: "Which chunking strategy was used for the documents"
    - Search Type: "dense: vector similarity search | sparse: BM25 keyword search | hybrid: combines both with alpha weighting"
    - LLM Model: "OpenAI model for generating answers (e.g., gpt-4o-mini, gpt-4o, gpt-4-turbo)"
    - Response Mode: "single_shot: one retrieval + answer | multi_step: iteratively refines search queries for better context"
    - Top K: "Number of chunks to retrieve (1-50, typical: 3-10)"
    - Alpha: "Weight between dense (1.0) and sparse (0.0) search in hybrid mode"
    - Sparse Config: "BM25 embedding config for the sparse component of hybrid search"
    - Max Steps: "Maximum query refinement iterations in multi-step mode (1-10)"
    - System Prompt: "Custom instructions for the LLM (e.g., tone, format, domain constraints)"

    Style: Add help text as a `<p className="mt-0.5 text-xs text-text-muted">` element after each label span.
  </action>
  <verify>npm run build passes with no TypeScript errors</verify>
  <done>AC-2 satisfied: All config form fields have descriptive help text</done>
</task>

<task type="auto">
  <name>Task 3: Verify PDF upload works at runtime</name>
  <files>main.py</files>
  <action>
    1. Check that pypdf is importable: run `python3 -c "from pypdf import PdfReader; print('OK')"`
    2. If pypdf is not installed, add it to requirements.txt and note the gap
    3. Review the PDF extraction code in main.py document upload endpoint for any issues
    4. Ensure the error message from PDF parsing failures is returned to the frontend (not swallowed)
    5. No code changes expected unless a bug is found
  </action>
  <verify>python3 -c "from pypdf import PdfReader; print('pypdf OK')" succeeds</verify>
  <done>AC-3 satisfied: PDF upload code verified correct, pypdf available</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- frontend/src/components/setup/* (Setup page — just completed)
- frontend/src/components/test/* (Test tab — Phase 17)
- frontend/src/components/experiment/* (Experiment — Phase 18-19)
- Backend API endpoints (no endpoint changes needed)
- DocumentUpload.tsx (upload component works correctly)

## SCOPE LIMITS
- Help text only — no interactive tooltips or modal documentation
- Fix delete error handling only — do not refactor delete confirmation UI
- Do not add new form validation rules

</boundaries>

<verification>
Before declaring plan complete:
- [ ] npm run build passes with no errors
- [ ] All 4 delete handlers have error feedback
- [ ] All config form fields have help text
- [ ] pypdf import verified
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Delete buttons provide clear feedback, help text guides users
</success_criteria>

<output>
After completion, create `.paul/phases/15-build-tab-polish/15-01-SUMMARY.md`
</output>
